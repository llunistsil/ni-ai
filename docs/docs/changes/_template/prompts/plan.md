Составь `docs/changes/<KEY>/plan.md` для намерения `docs/changes/<KEY>/intent.md`.
У тебя есть доступ к репозиторию.

Правила:
- Ты не проектируешь. Ты раскладываешь intent в порядок работ и сверяешь его с фактическим
  состоянием репозитория.
- Каждый путь, команда и конфиг, на которые ссылается план, обязаны существовать —
  проверь и приведи результат проверки с путём. Ничего не изобретай сверх intent:
  ни файлов, ни полей, ни зависимостей.
- Реальность разошлась с intent → раздел «Отклонения»: расхождение плюс один предложенный
  вариант. Молча не чини и не выбирай.
- Развилка с реальной альтернативой (тест — `docs/decisions/README.md`) → черновик ADR
  proposed, в плане — ссылка. Сам не выбирай.
- Структура плана: Источник (intent @ commit) → Проверки по репозиторию (результаты) →
  Файлы, которые изменятся (точные пути, new / правка) → Порядок работ (каждый шаг
  заканчивается «npm run verify зелёный») → Риски → Отклонения (раздел обязателен;
  «нет» — тоже ответ).
- Если изменение — эпик, дополнительно: список задач со слагами; зависимости и
  параллельность (параллельны только задачи без общих файлов после задачи-фундамента);
  зоны ответственности таблицей «модуль → задача-владелец»; контракты между параллельными
  задачами фиксируются в спеке задачи-владельца до их старта.
- Ничего не реализуй. Только план.
